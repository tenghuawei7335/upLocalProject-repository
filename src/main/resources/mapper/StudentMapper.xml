<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.thwcompany.demo.dao.StudentMapper">
    <select id="getAll" resultType="student">
        select ID id,NAME name,ADDRESS address,CLASSID classid from student
    </select>

    <select id="getById" parameterType="int" resultType="student">
        select ID id,NAME name,ADDRESS address,CLASSID classid  from student where ID=#{id,jdbcType=VARCHAR}
    </select>
</mapper>